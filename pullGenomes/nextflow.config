params {
      // Max resource options
      max_cpus = 3
      max_memory = 1.GB

      // Pull genomes
      taxon = "Escherichia coli" //"Enterobacterales"
      zipName = "ecoli.zip" //
      unzippedDir = "ecoli_data"
      pathToData = "ncbi_dataset/data" //We don't need to change This
      reference = "--reference" //Make empty string if we don't need it

      //tRNAscan
      filesFortSCAN = "$projectDir/results/relations.txt"
      domain = "-B"

      //general
      results = "results"

}

tower {
  accessToken = '69c9a8bbb2693ec12afa57a8e68bc9358a3f17c1'
  endpoint = 'http://localhost:8000/api'
  enabled = true
}
